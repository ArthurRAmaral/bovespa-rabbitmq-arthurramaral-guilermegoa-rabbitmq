version: '3.2'

services:
  api-gateway:
    image: bovespa/api-gateway:${TAG:-latest}
    build: ./api-gateway
    volumes:
      - ./api-gateway:/home/node/app
    ports:
      - '3333:3333'
    depends_on:
      - rabbitmq

  corretora:
    image: bovespa/corretora:${TAG:-latest}
    build: ./corretora
    volumes:
      - ./corretora:/home/node/app
    depends_on:
      - rabbitmq

  bolsa-de-valores:
    image: bovespa/bolsa-de-valores:${TAG:-latest}
    build: ./bolsa-de-valores
    volumes:
      - ./bolsa-de-valores:/home/node/app
    depends_on:
      - rabbitmq

  rabbitmq:
    image: rabbitmq:3-management-alpine
    logging:
      driver: none
    container_name: 'rabbitmq'
    ports:
      - 5672:5672
      - 15672:15672
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq/
      - rabbitmq-log:/var/log/rabbitmq

volumes:
  rabbitmq-data:
    name: rmqdata
  rabbitmq-log:
    # name: rmqlog

networks:
  default:
    name: rabbitmq
